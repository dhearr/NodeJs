// const fs = require(fs) // Core Module
const test = require("./test"); // Local Module
// const moment = require("moment"); // 3rd Party Module / Npm Module / node_modules

console.log(
  test.testNama("Dhea"),
  test.PI,
  test.mahasiswa.cetakMhs(),
  new test.Human()
);
